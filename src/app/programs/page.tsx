import ProgramsPage from "./programs-page";

export const metadata = {
  title: "Team Development Programs in Nigeria | StreetOps",
  description: ` StreetOps builds and runs structured team development programs for Nigerian organisations. See what we have created — and find out if we can build one for you.`,
};

const Page = () => {
  return <ProgramsPage />;
};

export default Page;
