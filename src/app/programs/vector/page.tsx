import VectorPage from "./vector-dynamic-page";

export const metadata = {
  title: "Staff Training Program in Lagos | Vector | StreetOps",
  description: ` The Vector Training Program is StreetOps' structured staff training initiative — designed to build real capability and drive lasting performance inside Nigerian teams.`,
};

const Page = () => {
  return <VectorPage />;
};

export default Page;
