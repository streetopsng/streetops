import ServicesPage from "./services-page";

export const metadata = {
  title: "Employee Engagement Services in Nigeria | StreetOps",
  description: ` StreetOps delivers hands-on employee engagement services for Nigerian organisations — diagnostics, manager training, culture design, and experience programmes.`,
};

const Page = () => {
  return <ServicesPage />;
};

export default Page;
